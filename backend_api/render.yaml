services:
  - type: web
    name: ethio-shopify-api
    runtime: ruby
    region: ohio
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "rails server -b 0.0.0.0 -p $PORT"
    healthCheckPath: /up
    envVars:
      - key: RAILS_ENV
        value: production
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: ethio-shopify-db
          property: connectionString
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      - key: PORT
        value: "10000"

databases:
  - name: ethio-shopify-db
    plan: free
    region: ohio
    postgresMajorVersion: 16
